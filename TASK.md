1. register user (save user with email, password) -> DONE
2. login user
    - create user_tokens table, using rails migration to create table -> DONE
    - authenticate user (create UserToken record) and response token